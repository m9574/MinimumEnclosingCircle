[project]
name = "circle"
version = "0.1.0"
description = "Simulation of minimum enclosing circle for a set of n points"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
]
